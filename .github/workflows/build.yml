name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: List files
        run: ls -R

      - name: Upload files
        uses: actions/upload-artifact@v4
        with:
          name: project-files
          path: .
